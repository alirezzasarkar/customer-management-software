import React from "react";

const Title = ({ title }) => {
  return (
    <div
      className="bg-gray-100 py-3 px-5 sm:mx-6 mb-5 font-bold text-lg text-green-600 rounded-md
"
    >
      {title}
    </div>
  );
};

export default Title;
